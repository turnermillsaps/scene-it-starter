// Render watchlist from localStorage to page

document.addEventListener('DOMContentLoaded', function(){
    var watchlistArr = [];
    
    function renderWatchlist() {
        watchlistHTML = localStorage.getItem('watchlist').map(function(movie){

        })
    }
})